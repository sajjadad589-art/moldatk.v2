import fs from 'node:fs';

const file = 'src/App.tsx';
let src = fs.readFileSync(file, 'utf8');

const oldImport = "import { loadSubscribers, upsertSubscriber, deleteSubscriber, loadInvoices, upsertInvoice, subscribeToGeneratorChanges } from './lib/sharedData';";
const newImport = "import { loadSubscribers, upsertSubscriber, deleteSubscriber, loadInvoices, upsertInvoice, loadLines, replaceLines, loadMonthlyTariffs, upsertMonthlyTariff, loadGeneratorSettings, saveGeneratorSettings, subscribeToGeneratorChanges } from './lib/sharedData';";
if (src.includes(oldImport)) src = src.replace(oldImport, newImport);

const oldLoad = `        const [cloudSubscribersRaw, cloudInvoices] = await Promise.all([\n          loadSubscribers(generatorId),\n          loadInvoices(generatorId),\n        ]);\n        if (disposed) return;`;
const newLoad = `        const [cloudSubscribersRaw, cloudInvoices, cloudLines, cloudTariffs, cloudSettings] = await Promise.all([\n          loadSubscribers(generatorId),\n          loadInvoices(generatorId),\n          loadLines(generatorId),\n          loadMonthlyTariffs(generatorId),\n          loadGeneratorSettings(generatorId),\n        ]);\n        if (disposed) return;\n\n        const localLines = readLocalJson<LineDistribution[]>('moldatk_lines', [], userSession);\n        const linesMarker = 'moldatk_cloud_lines_migrated_' + generatorId;\n        if (localStorage.getItem(linesMarker) !== '1' && cloudLines.length === 0 && localLines.length > 0) {\n          await replaceLines(generatorId, localLines);\n          setLines(localLines);\n        } else {\n          setLines(cloudLines);\n          localStorage.setItem(getStorageKey('moldatk_lines', userSession), JSON.stringify(cloudLines));\n        }\n        localStorage.setItem(linesMarker, '1');\n\n        const localTariffs = readLocalJson<MonthlyTariffRecord[]>('moldatk_monthly_tariffs', INITIAL_MONTHLY_TARIFFS, userSession);\n        const tariffsMarker = 'moldatk_cloud_tariffs_migrated_' + generatorId;\n        if (localStorage.getItem(tariffsMarker) !== '1' && cloudTariffs.length === 0 && localTariffs.length > 0) {\n          await Promise.all(localTariffs.map(record => upsertMonthlyTariff(generatorId, record)));\n          setMonthlyTariffs(localTariffs);\n        } else if (cloudTariffs.length > 0) {\n          setMonthlyTariffs(cloudTariffs);\n          localStorage.setItem(getStorageKey('moldatk_monthly_tariffs', userSession), JSON.stringify(cloudTariffs));\n        }\n        localStorage.setItem(tariffsMarker, '1');\n\n        const localSpecs = readGeneratorSpecsForSession(userSession);\n        const localInvoice = readLocalJson<InvoiceTemplateSettings>('moldatk_invoice_template', INITIAL_INVOICE_TEMPLATE, userSession);\n        const settingsMarker = 'moldatk_cloud_settings_migrated_' + generatorId;\n        const hasCloudSettings = Object.keys(cloudSettings.specs || {}).length > 0 || Object.keys(cloudSettings.invoiceSettings || {}).length > 0;\n        if (localStorage.getItem(settingsMarker) !== '1' && !hasCloudSettings) {\n          await saveGeneratorSettings(generatorId, localSpecs, localInvoice);\n        } else if (hasCloudSettings) {\n          const mergedSpecs = { ...localSpecs, ...cloudSettings.specs } as GeneratorSpecs;\n          const mergedInvoice = { ...INITIAL_INVOICE_TEMPLATE, ...localInvoice, ...cloudSettings.invoiceSettings } as InvoiceTemplateSettings;\n          setGeneratorSpecs(mergedSpecs);\n          setInvoiceTemplate(mergedInvoice);\n          localStorage.setItem(getStorageKey('moldatk_generator', userSession), JSON.stringify(mergedSpecs));\n          localStorage.setItem(getStorageKey('moldatk_invoice_template', userSession), JSON.stringify(mergedInvoice));\n        }\n        localStorage.setItem(settingsMarker, '1');`;
if (src.includes(oldLoad)) src = src.replace(oldLoad, newLoad);

const tariffSaveAnchor = `    try {\n      localStorage.setItem(getStorageKey('moldatk_monthly_tariffs'), JSON.stringify(normalized));\n      window.dispatchEvent(new Event('moldatk-local-sync'));\n    } catch (e) {}`;
const tariffCloudSave = `${tariffSaveAnchor}\n\n    if (userSession?.generatorId && (userSession.role === 'generator_admin' || userSession.role === 'collector')) {\n      void Promise.all(normalized.map(record => upsertMonthlyTariff(userSession.generatorId!, record))).catch(error => {\n        console.error('Failed to sync monthly tariffs:', error);\n        showToast('تم حفظ التسعيرة محلياً، وتعذرت المزامنة مؤقتاً');\n      });\n    }`;
if (src.includes(tariffSaveAnchor) && !src.includes('Failed to sync monthly tariffs')) {
  src = src.replace(tariffSaveAnchor, tariffCloudSave);
}

const invoiceHandler = `  const handleUpdateInvoiceTemplate = (template: InvoiceTemplateSettings) => {\n    setInvoiceTemplate(template);\n    try {\n      localStorage.setItem(getStorageKey('moldatk_invoice_template'), JSON.stringify(template));\n    } catch (e) {}\n    showToast('تم حفظ إعدادات القالب');\n  };`;
const sharedHandlers = `  const handleUpdateInvoiceTemplate = (template: InvoiceTemplateSettings) => {\n    setInvoiceTemplate(template);\n    try {\n      localStorage.setItem(getStorageKey('moldatk_invoice_template'), JSON.stringify(template));\n    } catch (e) {}\n    if (userSession?.generatorId && (userSession.role === 'generator_admin' || userSession.role === 'collector')) {\n      void saveGeneratorSettings(userSession.generatorId, generatorSpecs, template).catch(error => {\n        console.error('Failed to sync invoice template:', error);\n      });\n    }\n    showToast('تم حفظ إعدادات القالب');\n  };\n\n  const handleUpdateLinesShared = (newLines: LineDistribution[]) => {\n    setLines(newLines);\n    try {\n      localStorage.setItem(getStorageKey('moldatk_lines'), JSON.stringify(newLines));\n      window.dispatchEvent(new Event('moldatk-local-sync'));\n    } catch (e) {}\n    if (userSession?.generatorId && (userSession.role === 'generator_admin' || userSession.role === 'collector')) {\n      void replaceLines(userSession.generatorId, newLines).catch(error => {\n        console.error('Failed to sync generator lines:', error);\n        showToast('تم حفظ الكابينات محلياً، وتعذرت المزامنة مؤقتاً');\n      });\n    }\n  };\n\n  const handleUpdateGeneratorSpecsShared = (specs: GeneratorSpecs) => {\n    setGeneratorSpecs(specs);\n    try {\n      localStorage.setItem(getStorageKey('moldatk_generator'), JSON.stringify(specs));\n      window.dispatchEvent(new Event('moldatk-local-sync'));\n    } catch (e) {}\n    if (userSession?.generatorId && (userSession.role === 'generator_admin' || userSession.role === 'collector')) {\n      void saveGeneratorSettings(userSession.generatorId, specs, invoiceTemplate).catch(error => {\n        console.error('Failed to sync generator settings:', error);\n      });\n    }\n  };`;
if (src.includes(invoiceHandler) && !src.includes('handleUpdateLinesShared')) {
  src = src.replace(invoiceHandler, sharedHandlers);
}

// Replace the common line-update callback bodies with the shared cloud-aware handler.
src = src.replaceAll(`onUpdateLines={(newLines) => {\n            setLines(newLines);\n            localStorage.setItem(getStorageKey('moldatk_lines'), JSON.stringify(newLines));\n          }}`, `onUpdateLines={handleUpdateLinesShared}`);
src = src.replaceAll(`onUpdateLines={newLines => {\n                setLines(newLines);\n                try {\n                  localStorage.setItem(getStorageKey('moldatk_lines'), JSON.stringify(newLines));\n                  window.dispatchEvent(new Event('moldatk-local-sync'));\n                } catch (e) {}\n              }}`, `onUpdateLines={handleUpdateLinesShared}`);

src = src.replaceAll(`onUpdateGeneratorSpecs={(specs) => {\n            setGeneratorSpecs(specs);\n            localStorage.setItem(getStorageKey('moldatk_generator'), JSON.stringify(specs));\n          }}`, `onUpdateGeneratorSpecs={handleUpdateGeneratorSpecsShared}`);

src = src.replace(`onUpdateSpecs={newSpecs => setGeneratorSpecs(prev => ({ ...prev, ...newSpecs }))}`, `onUpdateSpecs={newSpecs => handleUpdateGeneratorSpecsShared({ ...generatorSpecs, ...newSpecs })}`);

fs.writeFileSync(file, src);
console.log('Shared lines, tariffs and settings cloud sync applied');
