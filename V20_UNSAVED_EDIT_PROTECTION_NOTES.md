# V20 Unsaved Edit Protection

تم إصلاح مشكلة رجوع الحقول بعد 30 ثانية أثناء تعديل البيانات.

## السبب
كان النظام يحدث بيانات الحساب في الخلفية كل 30 ثانية، وبعض نوافذ التعديل كانت تعيد تعبئة الحقول عند وصول تحديثات من الأب، فتضيع تعديلات المستخدم قبل الحفظ.

## الإصلاح
- منع تحديث بيانات الواجهة فوق أي نافذة تعديل مفتوحة.
- تهيئة FolderDetailModal عند فتح النافذة/تغيير المجلد فقط، وليس عند كل تحديث خلفي.
- تهيئة SubscriberModal عند فتح النافذة/تغيير المشترك فقط، وليس عند كل تحديث تسعيرة أو خطوط.
- إضافة data-moldatk-draft-form لتعريف نماذج التعديل المفتوحة.

## النتيجة
يبقى النص الذي يكتبه المستخدم داخل الحقول كما هو حتى يضغط حفظ أو يغلق النافذة.
